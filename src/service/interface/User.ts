// 1. Tipo para los datos del usuario
export interface User {
    iduser: number;
    name: string;
    last_name: string; // o usa un enum si prefieres
    email: string;
    phone: string;
    image: string;
    usuario: string;
}